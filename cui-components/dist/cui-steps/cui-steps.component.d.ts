export declare class CuiStepsComponent {
    /**
     * Steps to display (number, label, [active], [visited], [class])
     */
    steps: any[];
    /**
     * Optional alternate background (1)
     */
    alt: number;
    /**
     * Whether to display as vertical steps
     */
    vertical: boolean;
    /**
     * Whether to display as small steps
     */
    small: boolean;
}
